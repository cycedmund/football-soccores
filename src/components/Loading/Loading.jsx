const Loading = () => {
  return (
    <main className="flex h-[74%] items-center justify-center bg-slate-900">
      <span className="mx-auto p-10 loading loading-spinner loading-lg"></span>
    </main>
  );
};

export default Loading;
